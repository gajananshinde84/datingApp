1. Angular File
2. Angular Language Service
3. Angular SNippets
4. Angular2-switcher
5. Auto Rename Tag
6. Bracket Pair Colorizer
7. C#
8. C# Extension
9. Debugger for Chrome
10. Material Icon Theme
11. Nuget Package Manager
12. Prettier- Code Formatter
13. TSLint
14. install altertyjs for notificaiton- npm install altertiyjs
15. Angular JWT Token- # installation with npm
    npm install @auth0/angular-jwt
16. ngx-bootstrap for styling purpose(Design or look and feel). 
    npm install ngx-bootstrap --save
17. BootWatch -- themes
18. For Router we need to guard the routes by creating 
    routes.ts as mapping; 
    create auth guards ==>  ng g guard auth --skipTests ==> space==> CanActivate <--(Enter)
19. Entity Freamework Migration-
    dotnet ef migrations add ExtendedUserClass
    if anything wrong with migration then drop the database and re-create; in this case we will lose the data;
    also if your migrations class Up and Down method is coming empty then revert the snapshot method to last version;
    dotnet ef database drop ==> Y/N==> Y <-|
    dotnet ef migrations remove (will remove the last migrations that is not update in database)
    dotnet ef database update (This will udpate the existing migrations to database;)
    dotnet ef migrations add ExtendedUserClass
20. AutoMapper - Install it from Nuget Package Manager;

    
